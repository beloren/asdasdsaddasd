# v20 — единый стиль интерфейса и один билдер на весь UI

## Главное

* **Весь интерфейс в одном стиле** (по референсам Prospecting!/Fisch): тёмные полупрозрачные прямоугольные
  панели, тонкая цветная рамка у каждого окна, отдельная полоса заголовка с закладкой слева и красным
  курсивным «X» справа, жирный курсив с градиентом и тёмной обводкой, зелёные градиентные кнопки покупки,
  карточки с рамкой цвета редкости, заголовки секций «—— Money ——», жёлтые кнопки «Track», карточки
  ежедневных наград с галочками, рамкой-выделением и подписью под карточкой, кнопки HUD с подписями.
* **Один билдер** — `tools/BuildAllUI.lua` собирает в StarterGui **все** экраны игры (45 штук, список в
  `UI_V20_GUIDE.md`). Всё можно менять в Studio, игра использует именно эти экраны.
* **Всё на ImageLabel/ImageButton**: подложки окон, карточек, кнопок, слотов и т.д. Свои картинки ставятся
  в `UiTheme.Skins`/`UiTheme.Icons` (и `tools/ApplyUiSkins.lua` проставит их по всему StarterGui) или прямо
  на нужный элемент в Studio.
* **Мировой интерфейс тоже в едином стиле**: новый модуль `Shared/WorldUi` и шаблоны
  `StarterGui/WorldUiTemplates` — все надписи над рудой, тележкой, NPC, островами, сейфами, мобами, числа урона.
* Новые модули: `UiTheme`, `UiKit`, `UiRegistry`, `WorldUi`, папка `Shared/UiBuilders` (22 билдера).
  Старые билдеры экранов переписаны на UiKit.
* Удалены устаревшие `tools/Build*UI*.lua` (26 файлов) и `tools/BuildUIAssets.lua`: они собирали старый вид
  и при запуске перезаписали бы новый интерфейс. Вместо них — `BuildAllUI.lua`.

## Экраны, которые раньше собирались кодом прямо в скриптах, а теперь — билдерами (правятся в StarterGui)

Панель баффов, лента добычи и баннер редкого дропа, стрелка/шеврон квеста, окно шансов дропа, окно
островов и подписи над островами, мини-игра ритма, ячейка кристалла в руках, табличка руды под курсором,
подсказки установки тележки/предметов, кнопка наград за группу/избранное, карточки открытия жеод и сундуков,
«+$X» при начислении денег, кнопка шифтлока на телефоне, таблички гоблинов и валунов, обучение, диалог шахтёра.

## Магазин геймпассов

* Исправлены **огромные пробелы между категориями**: фон секции лежал внутри UIListLayout и удваивал её высоту.
* Карточки в 2 колонки, заголовки категорий «—— Money ——», цвет рамки по категории, описания товаров
  (`Config.Shop.Descriptions`), зелёные кнопки с ценой в Robux; купленный геймпасс показывает «OWNED» и больше не открывает окно покупки.

## Исправленные баги

* `ImageButton.Text` у кнопок Claim/Collect/Buy вызывал ошибку — подпись теперь в дочернем `Caption`.
* Книга-меню при очистке сетки удаляла `UIPadding` — чистятся только GuiObject.
* Одноразовый `FindFirstChild` в клиентах (диалог шахтёра, мини-игры и др.) иногда создавал **второй экземпляр**
  экрана, если копия из StarterGui приходила позже — теперь все экраны берутся через `UiRegistry.Get`, дубли удаляются.
* Окно прокачки не считалось модальным — теперь закрывает другие окна и блокирует ввод как остальные.
* `RubbleCrystalUI` 10 секунд ждал удалённый `PickaxeHotbar` и двигал несуществующий слот; создавал пустой ScreenGui.
* `BuildAllUI` больше не удаляет `MobBillboardTemplates` (без них таблички гоблинов собирались запасным кодом).
* Шрифт `Arcade` без кириллицы в мировых надписях и нескольких окнах заменён шрифтом темы — русский текст больше не превращается в квадраты.
* `BuildGeodeAssets.lua` пересобирал старое окно жеод поверх нового — теперь собирает его общим билдером.
* Русские переводы для новых подписей интерфейса (`Localization`, блок v20).

## Для разработчика

* Как добавить экран, где имена-контракты, как менять состояние кнопок — `UI_V20_GUIDE.md`.
* Предпросмотр экранов без Studio (для разработки): `tools/dev/preview_ui.luau` и `tools/dev/export_ui.luau` (Lune).

## Экономика (v20.1)

* Валуны: деньги за ролл 0.6 → 0.25 полной тележки, PERFECT ×1.6 → ×1.3; один ролл не больше
  0.5 полной тележки текущей пещеры игрока (`Config.Boulders.MoneyCapInCarts`) — дешёвая кирка
  больше не открывает фарм валунов, которые платят как пещеры далеко впереди.
* «Большие цифры» на верхних тирах (`Config.NumberGrowth`): начиная с 5-й руды каждая следующая
  дороже ещё в 1.19 раза. Полная тележка пещеры 15 ≈ $2.6M, цена пещеры 15 ≈ $3.9M.
  Цены пещер, тележки, кирки и престижа умножены на тот же масштаб дохода — время на каждый шаг
  прежнее, растут только числа. Цены пещер дополнительно +15%.
* Растяжка поздней игры (`Config.LateGameStretch`): цены пещер 9–15 и тележки/кирки тех же тиров
  ×1.35 → ×1.5 → ×1.65 → ×1.75. Первые ~45 минут (до пещеры 8 и первого престижа) не изменились;
  первое прохождение до пещеры 15 ≈ 3.3 ч по формуле (≈ 3.5–4 ч реальной игры) вместо ≈ 2.3 ч.

## Магазин (v20.4)

* Сверху — навигация по категориям (клик листает к секции).
* «Forever Pack» (`Config.Shop.ForeverPack`): цепочка бесплатная награда → пак → пак дороже, обновляется
  каждые 4 часа (таймер «Refresh in»); справа — большая карточка самого дорогого пака с вращающимися лучами.
* Категории по популярности: Cash, Boosts, Game Passes, Weather, Geodes, Dynamite, Special Offers, Skins.
  Карточки подкрашены цветом категории с обводкой; пустые места и пустые категории — карточки «?».

## Предметы в руке (v20.5)

* У каждого предмета в руке (кроме руды) — плашка над хотбаром: название цветом предмета и строка
  «что делает · как применить» (`Shared/ItemHints`, свои тексты — `Config.ItemHints[key]`). Кирка — 4 сек после взятия.
* Динамит: подсказка короче; фитиль на 1.5 сек дольше (Small 2.9 с, Bundle 3.3 с, Mega TNT 4.7 с).

## Шрифты в мире и трекер квеста (v20.8)

* Fredoka и засечный шрифт убраны: имена NPC (магазин, мэр/ребёрс и т.д.), подписи над трофеями и тотемами
  и трекер квеста теперь написаны тем же жирным курсивом, что и деньги в HUD (`Theme.Fonts.Number`).
* Заголовок квеста в трекере («Treasure Time» и т.п.) — 32 px вместо ~12 (строка 42 px), он крупнее остального текста.
* Нужно пересобрать: `BuildAllUI` с `ONLY = { "QuestUi", "WorldUiTemplates" }` (QuestUi MinVersion 24).

## Значки без «квадратиков», призрак сундука, офлайн-деньги (v20.9)

* В шрифтах Roblox нет ✔ ✕ ➤ ▲ ▼ ◈ ◇ ▶ ◀ ↻ ⟲ — они рисовались пустым квадратом. Новые `UiKit.Shape` /
  `UiKit.PaintShape` / `UiKit.GlyphToShape` рисуют галочку, крестик, стрелку, шевроны и ромбы из рамок.
  Исправлено: стрелка квеста у края экрана и шеврон под ногами, галочки/крестики требований престижа,
  ромбы трекера квеста, стрелки над NPC и в обучении, стрелка «BANK», галочки магазина/стартового набора/
  ежедневных наград, кнопки установки на телефоне. В тексте ✔ → ✅ (эмодзи рисуются), ▶/◀ → >/<.
* Сундук из руки ставится через призрак, как тотемы: зелёный/красный, по умолчанию смотрит на игрока,
  R — повернуть. Модель сундука общая: `PlaceableFactory.BuildChest`.
* Офлайн: вместо руды в тележке (лагало при высыпании) — деньги сразу на счёт (`Config.OfflineCart.Mode = "Money"`):
  5% активного дохода в минуту × минуты офлайна × множитель ребёртов, потолок 6 ч (≈ 18 мин игры).
  Старая схема — `Mode = "Ore"`.
* Пересобрать: `BuildAllUI` (QuestUi, QuestMarkerUi, RebirthDialogButtons, ShopUi, DailyRewardUi, StarterPackOffer,
  PlacementUi, TutorialUi, WorldUiTemplates, SkinUi, PerkUi, GeodeUi, IslandUi, DropPreviewUi, UpgradeShopUi) —
  или просто весь BuildAllUI; устаревшие копии и так пересоберутся кодом при запуске.

## Окно жеод (v20.10)

* Убрана вкладка «<жеода> DROPS» со списком шансов. Когда жеода выбрана, в правой колонке окна
  выезжают две кнопки: «ALL DROPS» (окно шансов с 3D-предпросмотром) и под ней большая «CRACK».

## Промпты в мире (v20.11)

* Надпись действия («Hold To Open» у сундука и т.п.) всегда в одну строку; когда есть вторичная кнопка
  («[G] View Drops»), надпись чуть выше, кнопка под ней — больше не перекрываются.

## Подписи над тотемами и трофеями (v20.12)

* Подпись — «табличка» в мире: размер BillboardGui в стадах (Scale) + TextScaled, как советуют на DevForum.
  Держит один размер относительно предмета (не раздувается при отдалении камеры); вплотную рост
  ограничен `DistanceLowerLimit = 8`. Размеры — `LABEL_WIDTH/LABEL_TITLE_HEIGHT/LABEL_LINE_HEIGHT` в BaseDecorService.
  Билборды из своих моделей Assets больше не переделываются — остаются как собраны.

## Окно жеод — кнопки (v20.12)

* «ALL DROPS» и «CRACK» — на своей подложке (карточка окна), крупнее (58 и 78 px), подписи фиксированного
  размера 26/38 (TextScaled без переноса в Roblox сжимал их до нечитаемых).

## Документация (v20.13)

* `PLACEHOLDERS_GUIDE_v20.pdf` — полный гайд по плейсхолдерам: точные имена ассетов, структура моделей и маркеров,
  VFX, погода, звуки (с текущими Id и пустыми), музыка, анимации, картинки (UiTheme + Config), экраны UI, чек-лист.
  Генератор — `tools/dev/docs/make_placeholders_pdf.py` (списки из Config).

## Предложения «группа / избранное» (v20.14)

* Окна наград за группу и избранное всплывают не сразу после удачи (сундук, продажа, редкая руда, престиж),
  а через 20–30 сек (`Config.SocialOffers.DelayMin/DelayMax`). Новая удача за это время отодвигает показ;
  если игрок занят (мини-игра шахты, волна гоблинов, рагдолл, апгрейд, обучение) — ждём, но не дольше
  `MaxWaitSeconds` (180 сек), иначе предложение переносится на следующую удачу.

## Инвентарь (v20.15)

* Клик по предмету (в сетке инвентаря и в хотбаре) — взять в руки, повторный клик — убрать.
* Зажатие (≥ 0.25 сек) или зажатие с движением — перетаскивание: в хотбар, между слотами, обратно в сетку.
  Старый режим «клик — взял, клик — положил» убран.

## Две версии UI: ПК и телефон (v20.16)

* Новый `shared/UiLayout` + `client/ResponsiveUi` — одна система подгонки вместо разрозненных DeviceScale/MobileScale/
  AutoScale/ResponsiveScale в разных скриптах (они спорили друг с другом и местами уменьшали окно дважды).
* Профиль устройства: **Phone** (сенсор без клавиатуры, короткая сторона ≤ 600), **Tablet**, **Desktop**.
  Проверить телефонную версию на ПК: `Config.UiLayout.ForceProfile = "Phone"`.
* Любое окно и любая плашка всегда **целиком влезают** в безопасную зону экрана (отступ `Margin`). HUD на телефоне
  мельче (`HudScale.Phone`), окна — максимально крупно, насколько позволяет экран. Анимации открытия (Pop/PanelScale/…)
  работают как раньше — их масштаб домножается на подгонку.
* Телефонная раскладка (`Config.UiLayout.Overrides.Phone`): деньги/престиж — правый верх (не в зоне джойстика),
  баффы — рядом, лента добычи — под деньгами, квест — левый верх, снаряжение — ряд над хотбаром, подсказки тележки/NPC —
  над ним, предложения — левее кнопки прыжка. Любую позицию можно поправить в Config без кода.
* Свой масштаб элемента на телефоне — атрибут `UiScale_Phone` в Studio; исключить элемент — атрибут `NoAutoFit`.

## Пролаг при перелёте камеры к руде (v20.16)

* Анимация масштаба шахты (растяжение на ударах, взрыв при выбросе, «плевок» на каждый кусок) теперь проигрывается на
  клиенте (`client/MineScaleFX`): сервер больше не гоняет `Model:ScaleTo` всей шахты каждый кадр по сети — именно это
  давало пролаг при выбросе руды. Вернуть старое поведение: `Config.MineExpedition.ClientMineScale = false`.

## Курс руды и подписи островов (v20.17)

* Плашка «ORE x1.00 · таймер» сверху экрана убрана (`Config.Merchant.ShowTicker = false`). Курс и таймер нового
  товара теперь только на табличке над торговцем у банка, где сдаёшь руду (`ShowBoard = true`), видна с 90 стадов.
* Табличка торговца и подписи над островами — «таблички в мире»: размер в стадах + TextScaled, шрифт денег.
  Подпись острова меньше (20×5 стадов), вплотную не раздувается (`DistanceLowerLimit`), издалека не исчезает
  (`DistanceUpperLimit` = 220). Пересобрать: `BuildAllUI` с `ONLY = { "IslandUi" }` (или старый вид соберётся кодом).

## Исправление телефонной версии (v20.18)

* v20.16 перехватывала чужие UIScale (анимации окон, наведение кнопок) и домножала их — это давало
  «Exponential deferred event growth … ResponsiveUi», дёрганье, пропажу HUD и меню, мелкие подсказки (и на ПК).
  Перехват убран полностью; скрипты окон/HUD/подсказок возвращены к прежнему (рабочему) виду.
* `ResponsiveUi` теперь работает **только на телефоне** и масштабирует лишь верхние элементы БЕЗ своего UIScale
  (свой — `PhoneFitScale`, в него пишет только он, без подписок). Кнопки и окна со своими масштабами не трогает.
  На ПК и планшете не делает ничего.
* Квест на телефоне больше не прячется за верх экрана (трекер не вкладывается в HUD, стоит слева вверху).
* Катсцены (CinematicHud) возвращают HUD на телефонную позицию, а не на ПК-позицию.
* Подсказки тележки/NPC на телефоне крупнее (×0.95 вместо ×0.75), подсказка предмета в руке — полного размера.

## Ложное «собран старым билдером» (v20.18)

* `UiRegistry.Build` ставит `UiKitVersion` не ниже `MinVersion` реестра. Раньше экраны, чей билдер сам версию не пишет
  (PerkUi, GeodeUi, GearUi, TutorialUi и др.), получали общую версию UiKit (20) и после подъёма MinVersion считались
  устаревшими даже сразу после BuildAllUI. После синхронизации запусти `tools/BuildAllUI.lua` ещё раз — предупреждения уйдут.

## Банк, торговец, валуны, плавильня (v20.19)

* Плашка «ORE x1.05 · таймер» сверху выключена надёжно: сразу при получении экрана и прямо в билдере
  (`Config.Merchant.ShowTicker = false`; MarketTicker MinVersion 21 — старая копия в StarterGui пересоберётся).
* Табло над торговцем вдвое крупнее (14×5.2 стадов) и цветное: название золотым, «ORE PRICE» золотым + курс цветом
  рынка, таймер голубым со временем белым.
* **Отдельная зона продажи**: в плейсхолдере банка `SellZone` — квадрат 18×18 перед зданием с неоновой рамкой и
  надписью «SELL ORE»; торговец (`MerchantSpot`) — справа за краем зоны. Без маркера торговец тоже встаёт справа
  ЗА зоной, а не внутри неё — к нему можно подойти, не продавая руду.
* Плашка ХП валуна — только после первого удара (целый валун рядом её не показывает).
* Надпись над плавильней — табличка в мире как над тотемами (7×1.85 стадов, TextScaled, шрифт денег).

## Телефон и квесты (v20.20)

* **Дёрганье UI на телефоне**: профиль устройства больше не прыгает «телефон ↔ ПК» (раньше учитывался PreferredInput,
  который меняется от любого движения мыши/касания) — считается один раз. Ряд кнопок топбара больше не масштабируется
  (его размер задаёт Roblox), hover-эффект кнопок выключен на сенсорных экранах.
* **Инвентарь**: кнопка 🎒 последним слотом хотбара — открыть/закрыть (на ПК ещё «~»).
* **Навигация квеста**: стрелка/луч/знак над целью по умолчанию ВЫКЛЮЧЕНЫ — клик по квесту в трекере включает,
  повторный — выключает (значок справа: «>» / «X»); Track в окне квестов тоже включает. Шеврон у ног игрока убран
  (`Config.QuestMarker.ShowChevron = true` вернёт).
* **Подсказка предмета в руке** — только текст с обводкой, без тёмной подложки; на телефоне меньше (×0.62).
* **Статусы** (баффы, погода, сейв-зона) на телефоне — правый низ, над кнопкой прыжка.
* **Топбар**: вместо кнопки квестов — кнопка магазина 🛒 (`client/ShopDockButton`); квесты — пункт QUESTS в меню-книге
  (SHOP в меню тоже остался). Пересобрать: `BuildAllUI` (CollectionMenu, GearUi) — иначе соберутся кодом.

## Иконки, перки, Prestige Mayor (v20.21)

* Кнопка магазина 🛒 в топбаре — теперь в билдере (`StarterGui/TopbarDock/Row/ShopDockButton`, иконка — ImageLabel
  `Icon`; картинка — `UiTheme.Icons.Shop` или прямо Image в Studio).
* Иконки на кнопках — ImageLabel `Icon` с эмодзи-запасным `Emoji` внутри (видно, пока Image пустой): награды за время
  (`UiTheme.Icons.Rewards`), ракетная кирка (`Rocket`), рюкзак в хотбаре (`Inventory`), узлы перков и святилищ
  (`ImageId` в Config.Prestige.Perks/Shrines или Image в Studio).
* Prestige Perks: узлы 58 px с шагом 30 — линии между улучшениями видны целиком, 4 узла влезают в ветку;
  «🗿 SHRINES» больше не переносится на две строки.
* Мэр: промпт «PRESTIGE» / «Prestige Mayor»; в своей модели надписи с «Rebirth» → «PRESTIGE», имя Humanoid над
  головой — «Prestige Mayor».
* Пересобрать: `BuildAllUI` (TopbarDock, DailyRewardUi, OfferUi, PerkUi) — иначе соберутся кодом.

## Новый декор, хранилище, банка для руды (v20.22)
* Декор у торговца: Tulip Patch (`Decor_Flowers1`), Sunflower Pot (`Decor_Flowers2`),
  Round Bush (`Decor_Bush1`), Berry Bush (`Decor_Bush2`) — у всех кодовые плейсхолдеры.
* Wooden Bench (`Decor_Bench`): промпт SIT сажает на `Seat` внутри модели (сесть может
  любой игрок); по касанию не садит. Нет `Seat` в своей модели — код добавит невидимый.
* Storage Chest (`Decor_StorageChest`): промпт OPEN (только владелец) → окно
  `StarterGui/DecorStorageUi` (билдер `UiBuilders/DecorStorageUi`, клиент
  `DecorStorageUI.client`): 10 ячеек-стопок, клик по руде — переложить, TAKE ALL / PUT ALL.
  Руда хранится в записи предмета (`PlacedDecor[].Storage`), переживает перезаход и смерть.
  Поднять сундук можно только пустым. Настройки — `Config.Placeables.Storage`.
* Ore Jar (`Decor_OreJar`): с рудой в руке — PUT ORE, кусок уходит в банку, уменьшенная копия
  крутится внутри (клиент `DecorJarSpin`, тег `DecorJarOre`), над банкой табличка — редкость
  цветом и название. TAKE ORE / подбор банки возвращают руду. Точка руды — `OreSpot`.
  Настройки — `Config.Placeables.Jar`.
* У рабочего декора «поднять» — вторая строка промпта на R (E занята основным действием).
* `InventoryService:TakeStackByUid` / `RoomFor` — снять стопку целиком / сколько влезет.
* PDF-гайд: таблица руды — имя ассета и редкость в отдельных колонках (без лишних символов),
  описание внешнего вида всего декора, раздел 8.5 «Рабочий декор», раздел 8.6 «Плавильня»
  (контракт `Smelter_Level1..4`, `furnace_active/inactive`, `Mouth`, остров, вид каждого уровня).

## Сейф пружинит при сборе денег (v20.23)
* После удачного COLLECT MONEY сервер увеличивает атрибут `CollectPulse` у сейфа (тег `SafeBounce`),
  клиент `SafeBounceFX` проигрывает локально пружинку: сплющился → подпрыгнул вверх → затухающее
  покачивание (~0.5 с), затем детали возвращаются ровно к исходным размерам и позициям.
  Работает и со своей моделью `Assets/GeodeSafe`. Форма пружинки — таблица `KEYS` в начале скрипта.

## Одна подсказка при установке предмета (v20.24)
* Верхняя плашка «Click a spot on your base to place • R rotate» (`PlacementUi/GhostHud/Hint`) скрыта —
  остаётся только подсказка предмета над хотбаром. Плашка всплывает на 1.5 с лишь с ошибкой
  «Only inside your own base!».

## Постройки на островах и мосты из-под земли (v20.25)
* Место и направление построек на островах (наковальня, подиум, сейф, плавильня):
  - свой остров — маркеры-модели `StationMarker` / `PodiumMarker` / `SafeMarker` (плита + стрелка,
    стрелка = куда смотрит постройка). Вставить их в модели островов: `tools/AddIslandMarkers.lua`
    (Command Bar), дальше таскать/крутить мышкой. Старые маркеры-детали тоже работают;
  - кодовый остров (или нет маркера) — `Config.Islands.Definitions.<Id>.Stations`
    (`Offset` от центра верха острова, `Yaw` в градусах).
* Мосты: любая модель в Workspace с именем `Bridge…` (`Config.Bridges.NamePattern`) спрятана под
  землёй и выезжает, когда игрок ближе `ShowDistance`: волна от ближнего края, перелёт и пружинка.
  Отошёл дальше `HideDistance` — подскок и плавный уход вниз с исчезновением. Локально у каждого
  игрока (client/BridgeRiseFX), со всем содержимым (детали, декали, свет, частицы, надписи).

## Погода: небо-градиент, эффекты по всей карте, гроза (v20.26)
* Небо без картинок-скайбоксов: `Atmosphere` (градиент горизонт → зенит), свои `WeatherColorCorrection`
  и `WeatherBloom`, звёзды/луна/солнце у `Sky`, по желанию `Clouds`; плавный переход за 6 с.
  Вид — `Config.WeatherEvents.Events[].Look` / `ClearLook`, или собранный в Studio в
  `Assets/Weather/<Id>/Look`. SunRays не используются. Старые Sky-ассеты — только при `UseSkyboxAssets = true`.
* Эффекты — библиотека `Assets/WeatherFX/<Имя>` (эмиттеры + звуки, атрибуты `Mode`/`Height`) и список
  `Effects` у каждой погоды. Встроенные: RainDrops, StormRain, BloodDrizzle, RainSplashes, Mist,
  Fireflies, StarDust, Embers, Ash, Pollen. Частицы живут в мире по клеткам карты вокруг камеры
  (дождь идёт по всей карте, а не едет за игроком), плотность падает к краю радиуса, смена погоды плавная.
  Пул падающих деталей-капель удалён. Своя текстура капли: загрузить `tools/dev/assets/rain_streak.png`
  и вписать ID в `Config.WeatherEvents.Fx.RainTexture`.
* Гроза: молнии в мире с ветками, вспышка неба, свет в точке удара, гром с задержкой по расстоянию
  (`Lightning.ThunderSoundIds`). Удар рядом с игроком: блоки травы разлетаются, падают и тают, пыль, искры,
  лёгкая тряска камеры.
* Банка для руды: подпись с редкостью строится первой и отдельно от модели руды — видна всегда.

## Торговец: вся мебель, скины «?», пополнение денег (v20.27)
* Вкладка BASE у торговца: показывается ВСЯ мебель (`Config.Placeables.DecorOrder`). Каждый цикл у каждой
  свой шанс оказаться в стоке — от `DecorMinChance` (редкая по `DecorWeights`) до `DecorMaxChance` (частая),
  или своё `Decor.<Id>.StockChance`. Не в стоке — строка затемнена, в конце списка, купить нельзя.
* Меню SKINS: после своих скинов — все ещё не открытые кирки карточками «?» (видна только редкость цветом),
  затемнены и не нажимаются. Сервер отдаёт их в `SkinService:GetState().Locked`.
* Офлайн-доход: при закрытии экрана возвращения монетки вылетают из кнопки COLLECT, по дуге влетают в
  счётчик денег (он подпрыгивает) и всплывает «+$X» — `shared/CoinShower` (`CoinShower.Play(amount, from)`).

## Торговец: каталог, «напомни мне», жеоды, скидка дня, «???», бонусы скинов (v20.28)
* Каталог: все товары видны всегда; чего нет в стоке — затемнено и в конце списка (как мебель).
* «Напомни мне»: звёздочка на товаре (до `Wishlist.MaxWishes`), хранится в профиле (`MerchantWishlist`).
  Новый цикл, отмеченное в стоке — уведомление и пульсирующее «⭐ IN STOCK!» над торговцем, гаснет при открытии лавки.
* Жеоды в стоке: `Geode_Own` (своей пещеры), `Geode_Next` (следующей), `Geode_Jackpot` (+3 тира, редко).
  Цена — `PriceMinutes` минут дохода игрока; покупка сразу в хранилище жеод.
* Скидка дня: один товар вкладки SHOP на сутки (UTC), всегда в стоке, −30% (`Config.Merchant.DailyDeal`),
  плашка «DEAL −30%» и зачёркнутая старая цена.
* Товар «???»: случайная награда (`Config.Merchant.Mystery.Pool`: зелье, жеода, мебель, деньги, редкая кирка),
  показывается карточкой открытия, как из сундука.
* Скины: бонусы переразложены по «бюджету» редкости (Common 0.10 … Mythic 0.62), у каждой кирки роль
  (`Config.SkinRoles`). В лавке — бонусы цветом и в скобках значения надетой кирки; в меню SKINS — роль.

## Сундук цикла — верхняя карточка торговца (v20.29)
* Самая верхняя карточка вкладки SHOP — сундук, редкость выпадает каждый цикл по
  `Config.Merchant.FeaturedChest.Weights` (Common 45 / Rare 32 / Epic 17 / Legendary 6), цена —
  `PriceMinutes[редкость]` минут дохода игрока. Покупка — сундук в инвентарь.
* Карточка выделена: подсветка цветом редкости с бегущим бликом, мерцающая рамка, вращающиеся лучи
  (как у верхней карточки магазина), крутящийся 3D-сундук выпавшей редкости (`Assets/Chests/Chest_<Редкость>`),
  лента FEATURED и покачивающиеся монетки. Сменилась редкость — карточка пересобирается.

## Фоны за товарами — картинки, «всегда в стоке», DEAL без подложки (v20.30)
* Нарисованные лучи за товарами заменены картинками `UiTheme.Backdrops` (Star, Burst — вспышка-звёздочка,
  Shine — блик, BlackLightning — чёрная молния, Spiral — спираль). Какая где — `UiTheme.RarityBackdrop`:
  Common/Uncommon и обычные товары магазина — блик; Rare — звезда; Epic/Legendary и деньги («лучшая цена») —
  вспышка-звёздочка; Mythic/Secret — чёрная молния (не перекрашивается); «???» — спираль.
  Общая функция `UiKit.Backdrop` / `UiKit.PaintBackdrop`.
* Где: магазин за Robux (карточки, цепочка Forever Pack, Mega Cash), карточки открытия жеод/сундуков
  (теперь с Rare), торговец — сундук цикла, лимитки, «???». Пересобрать ShopUi и RevealCards (BuildAllUI).
* Торговец: «DEAL −30%» — без подложки, красным текстом. Сундук цикла, Storage Chest и Ore Jar — всегда в стоке
  (`AlwaysInStock`).

## Офлайн-деньги — обычное появление монет (v20.31)
* По COLLECT на экране возвращения сервер проигрывает ту же процедуру, что при сборе денег в банке:
  3D-монетки у игрока (`BankService:SpawnCoinBurst`) и «+$X» (`MoneyGainEvent`). Экранный дождь монет
  (`shared/CoinShower`) там больше не используется (модуль оставлен для других мест).

## Фоны за товарами — полоски с прозрачным фоном (v20.31)
* Картинки с чёрным фоном заменены: `Rays` (10637682258) — основные полоски почти везде; `RaysSoft`
  (15600777303) — мягче, для Common/Uncommon; `Rarest` (11364097059) — Legendary/Mythic/Secret,
  красится в цвет редкости, у «???» — случайный цвет. Все фоны перекрашиваются (ImageColor3).
* Магазин за Robux: полоски за карточками геймпассов и остальных товаров теперь крутятся.

## Фоны: новые картинки для редких (v20.32)
* `UiTheme.Backdrops`: Mythic/Secret и «???» — `Rarest` 17230437889; Legendary — `Epic2` 15513493035.

## Мини-игра входа в шахту: фон за карточкой редкости (v20.33)
* Лучи за карточкой редкости (Rare+) — теперь та же картинка-фон, что за товарами магазина
  (`UiTheme.RarityBackdrop`: Rare/Epic — полоски, Legendary — `Epic2`, Mythic — `Rarest`), в цвет редкости,
  вращается и проявляется вместо неоновых брусков.

## Магазин: свой фон у каждой категории, Forever Pack — почти самое редкое свечение (v20.33)
* `UiTheme.CategoryBackdrop`: у каждой вкладки своя картинка и вращение (скорость/направление) —
  Forever (Epic2), Cash, Boosts, Passes, Weather, Geodes, Dynamite, Deals, Skins. Цвет — цвет категории.
* Forever Pack (Mega Cash и цепочка) — `Epic2` (15513493035), как у Legendary. Применяется и к старой
  сборке StarterGui (клиент перекрашивает на лету).

## Шахта лицом вперёд, банка-коллекция, заливка PICK UP, сейф-зона над участком (v20.34)
* Шахта: визуальный перёд модели поворачивается к внешней стороне (`Config.Mine.ModelFrontYaw = 90`,
  не туда — поставьте -90/180/0; деталь `MineFront` в модели главнее). Направление лица пишется атрибутом
  `MineFrontDir` на модель — катсцена улучшения тира снимает шахту С ЛИЦА, а не со стороны банка.
* Банка для руды: над ней редкость, название и стоимость руды ($).
* Вторичная строка промпта («[R] Pick Up» и т.п.) заливается зелёным, пока держишь клавишу/после тапа.
* Участок и сейф-зона — весь столб над прямоугольником PlotTemplate любой высоты (`GroundCheck.InPlot`):
  PvP-защита и установка предметов работают на крышах, островах, мостах, в прыжке.

## Эффекты вместо лучей, аура престижа, банка «1/N» (v20.35)
* Редкая руда (валуны, золотой валун, мутации): вместо столба света — ОДНА вспышка при приземлении
  (`shared/RevealVfx`). Свои: `Assets/Reveal_<Редкость>`, `Reveal_Mutation`, `RevealVFX`.
* Сундук: вместо столба — `Reveal_Chest_<Редкость>` → `Reveal_<Редкость>` → плейсхолдер.
* Золотой валун: столб света выключен (`Config.GoldenBoulder.ShowBeam = false`); разлом — свой
  `BoulderBreakVFX_Golden`. Валун любого тира — `BoulderBreakVFX_<тир>`; свои эффекты не перекрашиваются.
* Престиж: аура-корона вокруг игрока, видят все (`client/PrestigeAuraFX`, атрибут `PrestigeFxAt`);
  свой — `Assets/PrestigeVFX`.
* Банка для руды: первая строка — шанс «1/N» цветом редкости (как в катсцене шахты), потом название и $.

## Обучение: новые тексты и шаги, подсветка карточек; тишина в мини-игре (v20.36)
* Тексты обучения и подсказок — короткие, без длинных тире. Первый диалог: «The mine broke! Mine some stone
  to fix it.» (закрывается «Далее»), дальше «разбей валуны».
* Новые шаги: после мини-игры «собери руду, наступив на неё» (CollectOre), после постановки тележки
  «подойди к тележке, руда сама ляжет в неё» (LoadCart). Страховки от тупиков: полный рюкзак / пустой рюкзак.
* Починка шахты и первое улучшение: в окне улучшений за нужной карточкой крутятся лучи (красные у шахты,
  золотые у самого дешёвого доступного улучшения). Только в обучении (`Steps[].Highlight`, `HighlightColor`).
* Счётчики обучения: `OrePickedUp` (подбор руды), `OreDepositedToCart` (руда в тележке); `Steps[].ResetCounters`.
* Мини-игра шахты: пока она идёт (`MineExpeditionActive`), серверные тосты и лента/баннер добычи молчат
  (ошибки и Critical — показываются).

## Сток торговца (v20.37)
* Бесконечный сток — только у Storage Chest и Ore Jar. Сундук цикла и всё остальное после покупки уходит
  из стока (затемняется), до следующего цикла.

## v20.38 — полоски за карточкой шахты у каждой редкости; пак денег при повторном клике

- **Итоговая карточка мини-игры шахты:** у КАЖДОЙ редкости свои полоски из картинок UiTheme.Backdrops (раньше были только у Rare+, у остальных — неоновое кольцо). Таблица `Config.MineExpedition.RarityCard.Backdrops`: Common — RaysSoft; Uncommon — Rays; Rare — Rays + RaysSoft навстречу; Epic — Epic2 + RaysSoft; Legendary — Epic2 + Rays; Mythic — Rarest + Epic2. Слои крутятся в разные стороны (Spin), у второго свой размер (Scale). Неоновое кольцо-волна заменено на расходящиеся полоски той же картинки. Цвет — цвет редкости.
- **Не хватает денег → повторный клик:** первый клик — обычное «Not enough money». Если игрок в течение 6 с жмёт ТУ ЖЕ покупку ещё раз, открывается окно покупки Money Pack'а, которого хватит на недостающую сумму (самый дешёвый подходящий; не хватит ни одного — самый крупный). Работает на прокачке, у торговца, при покупке динамита, на островах и при престиже. Настройки — `Config.MoneyPackOffer` (RepeatSeconds, CooldownSeconds = 20, Packs). Во время мини-игры шахты окно не показывается.
